# Database Connection Settings
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/asha_boutique1?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Server Port Configuration
server.port=8080

# JWT Custom Properties
app.jwt.secret=${APP_JWT_SECRET:YOUR_JWT_SECRET_KEY_AT_LEAST_256_BITS_BASE64_ENCODED}
app.jwt.expiration-ms=86400000

# Multipart File Upload Properties
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Razorpay Credentials
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TNMU2P7Hh0Qyn8}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:TpMPte1OYIqBMXvxlyjZB02b}

# Demo UPI Configuration
demo.upi.id=${DEMO_UPI_ID:ashaboutique@okaxis}
